<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <include android:id="@+id/contentWithTitle"
        layout="@layout/dialog_common_content_with_title"
        android:visibility="gone"/>

    <include android:id="@+id/contentNoTitle"
        layout="@layout/dialog_common_content_no_title"
        android:visibility="gone"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/cancel"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            style="@style/DialogCancel"/>

        <TextView
            android:id="@+id/ok"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            style="@style/DialogOk"/>
    </LinearLayout>
</LinearLayout>